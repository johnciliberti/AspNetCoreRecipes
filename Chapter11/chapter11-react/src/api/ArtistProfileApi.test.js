/* import ArtistProfileApi from './ArtistProfileApi';

describe('Artist Profile Api', () => {
  beforeEach(() => {
    global.fetch = jest.fn().mockComponent( () => Promise.resolve(
      {
    ok: true, result: '12345', artistName: 'John'}}}));
});
});*/
